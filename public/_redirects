# Netlify _redirects file for SPA routing
# This is a fallback configuration that works alongside netlify.toml
# All routes should redirect to index.html with a 200 status code
# This allows React Router to handle client-side routing

/*    /index.html   200
